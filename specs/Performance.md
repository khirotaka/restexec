# パフォーマンス特性

## レスポンスタイム

| シナリオ | 目標レスポンスタイム |
|---------|------------------|
| 単純な計算 | < 200ms |
| ファイルI/O含む | < 500ms |
| 複雑な処理 | < 2000ms |
| タイムアウト | = timeout + 100ms |

## スループット

- **想定**: 10 req/sec (単一コンテナ)
- **制約**: Node.jsのイベントループ
- **スケール**: 水平スケール可能（ステートレス）